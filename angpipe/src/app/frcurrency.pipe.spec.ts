import { FrcurrencyPipe } from './frcurrency.pipe';

describe('FrcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FrcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
